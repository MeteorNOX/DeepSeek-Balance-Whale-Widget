//! 模块化气泡领域
//!
//! 整块气泡由若干「行」组成，每行可以并排放置多个模块（文本 / 余额 / 今日已用 /
//! 峰谷倒计时 / 超链接 / 图片动图）。气泡组是用户资源，落在
//! `<数据目录>/bubble/<组名>/group.json`，媒体与字体资源与组目录同处 `bubble/`。
//!
//! 与「应用配置」的关系：配置聚合里的 `bubble` 字段就是本领域的模型，
//! 因此 `domain::config` 依赖本领域（同层依赖，方向由聚合 → 组成部分）。

pub mod model;
pub mod repository;
